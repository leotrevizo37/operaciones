export type HostContext = { protocolVersion: '1.0'; moduleId: string; locale: 'es-MX'; timezone: string; tenantIds: string[]; period: { from: string; to: string }; identity: { subject: string; displayName: string; roles: string[]; permissions: string[]; tenantScope: string[] }; apiBaseUrl: string; auth: { getAccessToken: (moduleId: string) => Promise<string> }; navigate: (target: { moduleId: string; path?: string }) => void }
export type Manifest = { protocolVersion: '1.0'; moduleId: string; displayName: string; releaseStage: string; dataEnvironment: string; freshnessMode: string; clearance: string; tenantScope: string }
export type Summary = { sensorsObserved: number; healthySensors: number; disconnectedSensors: number; lateSensors: number; avgMinutesWithoutReadings: number | null; maxMinutesWithoutReadings: number | null; latestAuditAt: string | null; latestReadingAt: string | null }
export type SensorException = { sensorId: string; locationName: string; deviceName: string; sensorName: string; disconnected: boolean; late: boolean; minutesWithoutReadings: number | null; auditAt: string | null; lastReadingAt: string | null }
export type TenantResult = { tenantId: string; tenantName: string; coverageStatus: 'AVAILABLE' | 'NO_DATA' | 'NOT_SUPPORTED' | 'UNAVAILABLE'; missingSources: string[]; current: Summary; previous: Summary; exceptions: SensorException[]; errorCode: string | null }
export type DashboardResponse = { generatedAt: string; from: string; to: string; tenants: TenantResult[] }
